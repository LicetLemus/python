# listas y numeros en python 

# Crear una lista

numeros = [1, 2, 3, 4, 5]

# max y min

maximo = max(numeros) # 5
minimo = min(numeros) # 1

# sumar los elementos de una lista

suma = sum(numeros) # 15